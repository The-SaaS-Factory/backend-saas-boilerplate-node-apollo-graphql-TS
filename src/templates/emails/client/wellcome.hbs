<html>
  <head>
    <title>{{title}}</title>
    <style>
      /* Estilos para el encabezado */ header { background: linear-gradient(to
      right, #4a47a3, #643cad); color: #fff; padding: 20px; text-align: center;
      } /* Estilos para el pie de página */ footer { background:
      linear-gradient(to right, #4a47a3, #643cad); color: #fff; padding: 20px;
      text-align: center; } /* Estilos para los enlaces del pie de página */
      footer a { color: #fff; text-decoration: none; margin: 0 10px; padding:
      20px; text-align: center; } /* Estilos para los párrafos de bienvenida */
      .welcome { text-align: center; margin: 20px; } /* Estilos para los iconos
      de redes sociales */ .social-icons { text-align: center; margin: 20px; }
      .social-icons a { display: inline-block; width: 30px; height: 30px;
      background-color: #333; color: #fff; border-radius: 50%; text-align:
      center; line-height: 30px; margin: 0 10px; text-decoration: none; }
    </style>
  </head>
  <body>
    <header>
      <h1>{{platformName}}</h1>
       
    </header>

    <main>
      <div class="welcome">
        <p>{{username}}, {{content}} </p>

      </div>
    </main>

    <footer>
      <p> {{platformDescription}}</p>
      <hr />
      <br />
      <p>{{platformAddress}}</p>
      <br />
      <p>{{platformEmail}}</p>

    </footer>
  </body>
</html>